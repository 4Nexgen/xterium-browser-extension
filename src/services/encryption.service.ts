export class EncryptionService {
    encryptPassword(decryptedPassword: string): string {
        return "return the encrypted password here..."
    }

    decryptPassword(encryptedPassword: string): string {
        return "return the decrypted password here..."
    }
}